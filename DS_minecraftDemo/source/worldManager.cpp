#include "worldManager.h"

void worldManager::intitialize()
{
	for(int i = 0, j = 0; i < MAX_CUBES; ++i)
	{
		// randomize type
			// if(air) don't store it
			// else, store it and increment j
	}
}

int worldManager::size()
{
	return numCubes;
}